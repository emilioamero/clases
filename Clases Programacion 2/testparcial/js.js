/* let zapatillas = [
  {
    marca: "Adidas",
    talle: {
      talle1: "38",
      talle2: "41",
    },
    serie: "14725885414",
  },
  {
    marca: "Nike",
    talle: {
      talle1: "35",
      talle2: "36",
    },
    serie: "8989854777",
  },
];

for (let index = 0; index < zapatillas.length; index++) {
  console.log(zapatillas[index].marca);
  let talles = zapatillas[index].talle;
  console.log(talles.talle1);
  console.log(talles.talle2);
}
 */

/* let cantidad = 10;
let cantidad2 = '10';

if (cantidad === cantidad2) {

} */

/* for (let index = 0; index < 10; index++) {
  document.write(index + "<br>");
  if (index == 5) {
    break;
  }

  if (index == 1) {
    continue;
  }
} */

/* let list = document.createElement("ul");
document.body.appendChild(list);
let frutas = ["Manzana", "Banana", "Naranja", "Pera"];
list.innerHTML = "";
for (let i = frutas.length-1; i >=0; i--) {
  let item = document.createElement("li");
  item.textContent = frutas[i];
  list.appendChild(item);
}
 */

/* let test = document.querySelectorAll('.clase');
console.log(test); */

/* let elementos =[];
let animalvolador ="Loro";
elementos.push('Perro');
elementos.push('Gato', animalvolador);
elementos.pop();
elementos.pop();
console.log(elementos.length); */

/* alert(convertirTexto('progRAMACIon 2',true));

function convertirTexto(texto, convertir) {
  let resultado = "";
  if (!convertir == false) {
    resultado= texto.toUpperCase();
  }else{
    resultado=texto.toLowerCase();
  }
  return resultado;
}
 */

/* let pais = "argentina";
if (pais[pais.length - pais.length] == pais[0]) {
 
}
 */

/* let pais = "argentina";
if (pais[pais.length - pais.length] == pais[0] && pais[pais.length - pais.length] == 'r') {
 
}else{
  console.log('false')
}
 */