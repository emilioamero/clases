-en el backend ejecutar npm install
-luego npmrun desarrollo

en la consola debera decir:
Servidor escuchando en http://localhost:3000 -> Esto es porque eservidor esta funcionando
Conexión exitosa a la base de datos MySQL -> esto es porque se conecto la base de datos


get todos los usuarios
http://localhost:3000/api/users






post 
http://localhost:3000/api/users

{
  "nombre": "Juan",
  "email": "juan@example.com"
}


put 
http://localhost:3000/api/users

{
  "nombre": "Juan",
  "email": "juan@example.com"
}




get solo uno 
http://localhost:3000/api/users/:id




-----------Crear Tabla usuarios------------------

CREATE TABLE `usuario` (
  `id` int NOT NULL AUTO_INCREMENT,
  `nombre` varchar(100) DEFAULT NULL,
  `email` varchar(100) NOT NULL,
  PRIMARY KEY (`id`)
) ENGINE=InnoDB AUTO_INCREMENT=10 DEFAULT CHARSET=utf8mb4 